# - name: Prompt
#   hosts: all
#   vars_prompt:
#     - name: username
#       prompt: What is your username?
#       default: "ram"
#       private: no

# - name: Debug output
#   ansible.builtin.debug:
#     msg: "Hello"

- name: Run playbook
  hosts: local
  vars:
    ssh_keypair_path: '~/.ssh/id-ram_ed25519'
  roles:
    - role: software
    - role: postinstall
      tags: [ postinstall ]
